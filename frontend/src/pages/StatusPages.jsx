export function UnauthorizedPage({ navigate }) { return <section className="status-page"><span>403</span><h1>You don’t have access to this area.</h1><p>Your account does not have the required role. Permissions are verified by the backend.</p><button className="button button--primary" onClick={() => navigate("/dashboard")}>Go to dashboard</button></section>; }

export function VerifyEmailPage({ navigate, search }) { const verified = new URLSearchParams(search).get("status") === "success"; return <section className="status-page"><span>{verified ? "✓" : "!"}</span><h1>{verified ? "Email verified" : "Verification link is invalid or expired"}</h1><p>{verified ? "Your account is ready. You can now sign in." : "Request a new verification link and try again."}</p><button className="button button--primary" onClick={() => navigate(verified ? "/login" : "/resend-verification")}>{verified ? "Go to sign in" : "Resend verification link"}</button></section>; }
export function NotFoundPage({ navigate }) { return <section className="status-page"><span>404</span><h1>This page can’t be found.</h1><p>The link may be outdated or the page may have moved.</p><button className="button button--primary" onClick={() => navigate("/dashboard")}>Go to dashboard</button></section>; }
